# Fixture Layout

- `images/`: generated local image fixtures (created by script)
- `markdown/`: consolidated markdown fixture with all cursor cases
- `html/`: consolidated html fixture with all cursor cases
- `explorer/`: nested files for explorer preview + `:SearchImage`

Generate local image files with:

```sh
python3 test/scripts/generate_test_images.py
```

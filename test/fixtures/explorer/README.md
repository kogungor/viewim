# Explorer Fixture Notes

Image files in this directory are generated by:

```sh
python3 test/scripts/generate_test_images.py
```

After generation, use this tree with `nvim-tree`, `oil`, or `neo-tree` to test:

- manual preview keymap
- optional mouse preview keymap
- optional auto-preview behavior
- hidden file behavior (`.hidden.png`)
